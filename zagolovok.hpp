#include <vector>

struct Level {
	std::vector<std::vector<int>> task;
	std::vector<std::vector<int>> ans;
};

extern Level level1;
extern Level level2;
extern Level level3;
extern Level level4;
extern Level level5;

extern Level now;
